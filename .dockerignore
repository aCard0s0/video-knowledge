**/target
.git
.idea
.vscode
docs
*.md
.DS_Store
data
# The whole console, not just its build output: no image built from the repo root needs it any
# more now that the server stopped baking it in. It has its own context and its own .dockerignore.
applications/webapp
# Anywhere, not just under the webapp: `ng` had also written a 7.1 MB cache at the repo root,
# and every image build was shipping it into the context.
**/.angular
**/out-tsc
/package
